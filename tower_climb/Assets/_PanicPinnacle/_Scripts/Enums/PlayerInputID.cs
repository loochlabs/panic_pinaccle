using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace PanicPinnacle.Legacy {
	/// <summary>
	/// An enum that was used with the old InputManager. Primarily so Legacy components don't break entirely.
	/// </summary>
	public enum PlayerInputID {
		One = 1,
		Two = 2,
		Three = 3,
		Four = 4,
	}
}